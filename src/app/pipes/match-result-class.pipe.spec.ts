import { MatchResultClassPipe } from './match-result-class.pipe';

describe('MatchResultClassPipe', () => {
  it('create an instance', () => {
    const pipe = new MatchResultClassPipe();
    expect(pipe).toBeTruthy();
  });
});
