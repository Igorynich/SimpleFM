import { MatchOpponentPipe } from './match-opponent.pipe';

describe('MatchOpponentPipe', () => {
  it('create an instance', () => {
    const pipe = new MatchOpponentPipe();
    expect(pipe).toBeTruthy();
  });
});
