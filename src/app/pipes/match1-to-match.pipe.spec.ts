import { Match1ToMatchPipe } from './match1-to-match.pipe';

describe('Match1ToMatchPipe', () => {
  it('create an instance', () => {
    const pipe = new Match1ToMatchPipe();
    expect(pipe).toBeTruthy();
  });
});
