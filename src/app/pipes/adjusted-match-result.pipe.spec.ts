import { AdjustedMatchResultPipe } from './adjusted-match-result.pipe';

describe('AdjustedMatchResultPipe', () => {
  it('create an instance', () => {
    const pipe = new AdjustedMatchResultPipe();
    expect(pipe).toBeTruthy();
  });
});
