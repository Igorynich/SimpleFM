import { LeagueTourToWeekPipe } from './league-tour-to-week.pipe';

describe('LeagueTourToWeekPipe', () => {
  it('create an instance', () => {
    const pipe = new LeagueTourToWeekPipe();
    expect(pipe).toBeTruthy();
  });
});
