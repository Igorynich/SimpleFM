import { CupRoundToWeekPipe } from './cup-round-to-week.pipe';

describe('CupRoundToWeekPipe', () => {
  it('create an instance', () => {
    const pipe = new CupRoundToWeekPipe();
    expect(pipe).toBeTruthy();
  });
});
